Change feature to accept optional location reference (vertex, implicit mate connector)
Add resize manipulators for X size, Y size, Z size, and diagonal one that scales all 3
Allow resize tools to invert so that in corner mode we can create geometry in any direction

